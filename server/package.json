{
  "name": "hr-portal-server",
  "version": "1.0.0",
  "description": "JSON Server mock backend for HR Portal",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
